# print
    print('%s'%(x,y..))
    print('{0}{1}'.format(x.y..))
    print(f'{x}{y}')

# 格式化字符
    %s # 格式化字符
    %05d # 格式化整数，用0补足5位数，没有0表示用空格补足
    %5f # 格式化浮点数,小数点后保留5位
    %u # 格式化无符号整数
    %o # 格式化无符号八进制数
    %x # 格式化无符号十六进制数
    %X # 格式化无符号十六进制数（大写）
    %e # 用科学计数法格式化浮点数
    %p # 用十六进制数格式化变量的地址


# end
    print('x',end='')
